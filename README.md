# owner-signal-persona

> **DEPRECATED.** Every component has exactly two contracts —
> `signal-<component>` (ordinary) + `meta-signal-<component>` (meta). There is no
> `owner-signal-*` channel. This is the deprecated **OwnerSignal** form
> (OwnerSignal → MetaSignal, Spirit `hnpo`); its privileged surface belongs in
> **`meta-signal-persona`**. Persona's canonical pair is `signal-persona` +
> `meta-signal-persona`. Per psyche 2026-06-07 (Spirit `n0ss`).

The (deprecated) owner-only Persona surface: engine launch, catalog query,
retirement, component start, and component stop — folds into
`meta-signal-persona`.
